## ADHD'ed DHCP server _(whatever this means)_

### Build it:
- `nix build`/`cabal build`

### Run it:
- `$EDITOR config.dhall`
- `cabal run`/`nix run`/`./result/bin/adhd`

### Configuration
It's intuitive and short (./config.dhall)
